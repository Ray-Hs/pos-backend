model Tax {
    id   Int   @id @default(autoincrement())
    rate Float @default(0)

    invoices Invoice[]
}

model Service {
    id     Int @id @default(autoincrement())
    amount Int @default(0)

    invoices Invoice[]
}
